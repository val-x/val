{
    "name": "backend",
    "version": "2.1.3",
    "private": true,
    "scripts": {
        "prepare": "bun ../../../../scripts/prepare.js",
        "dev": "bun ../../../../scripts/prepare.js dev",
        "start": "bun ../../../../scripts/prepare.js start",
        "build": "bun ../../../../scripts/prepare.js build",
        "clean": "rimraf __pycache__ *.pyc venv"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "rimraf": "^5.0.5"
    },
    "dependencies": {
        "backend": "file:"
    }
}
